---
тема: Мигающий светодиод
автор: Григорьев Василий
дата: 2026-02-04
---
# Результат:
- [ ] На вашем компьютере установлен python
- [ ] На вашем компьютере установлена библиотека python для взаимодействия с COM-портом pyserial
- [ ] В проект ```02-on-off``` добавлен python-скрипт ```on_off.py```
- [ ] В python-скрипте настроен COM-порт
- [ ] Добавлена инструкция для пользователя. Например, ```e - switch on led / d - switch off led / q - end execution```
- [ ] При вводе в ==***python-терминал***== символа ```e``` - светодиод  включается, при вводе ```d``` - светодиод выключается
# Подготовка
- Проверить наличие python командой ```python --version ```, если нет то установить
- Установить ```pyserial``` для связи с COM-портом через python командой:

```bash 
pip install pyserial 
```

# Python-скрипт
- В проекте ```02-on-off``` создать скрипт ```on_off.py```
- Импортировать библиотеки для работы с последовательным портом  и с системными функциями:
``` python
import serial, sys
```
- Добавить параметры COM-порта:
``` python
serialName = 'COM14'
serialBaudRate = 115200
ser = serial.Serial(serialName, baudrate=serialBaudRate)
```
- Добавить вывод, обозначающий начало работы скрипта
``` python
print('start')
```

- Добавить инструкцию для пользователя для ввода параметров
- Добавить знакомую конструкцию, и реализовать логику мигания светодиодом из python-скрипта
``` python
try:
    while True:
	# код
finally:
	# код
	
```

> [!ATTENTION] Внимание!
> - [[Данные записываемые в COM-порт должны быть ascii]]
> - Документация на serial расположена по [ссылке](https://pyserial.readthedocs.io/en/latest/pyserial_api.html) 
> - При окончании работы программы не забудьте про закрытие COM-порта.

